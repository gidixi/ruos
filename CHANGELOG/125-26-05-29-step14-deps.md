# 125 — Step 14 deps: virtio-drivers + rand_chacha + smoltcp dhcp

**Data:** 2026-05-29

## Cosa
`virtio-drivers 0.13`, `rand_chacha` (no_std); smoltcp += `medium-ethernet` +
`socket-dhcpv4`.

## Perché
Driver virtio-net + CSPRNG ChaCha20 + client DHCPv4 su Ethernet (Step 14).

## File toccati
- kernel/Cargo.toml
