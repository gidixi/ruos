# 227 — Cell + CellAttr types

**Data:** 2026-06-03

## Cosa
Aggiunti `CellAttr` (bitflags: BOLD, DIM, UNDERLINE, REVERSE) e `Cell`
(ch/fg/bg/attr + costruttore `blank`) a `kernel/src/console/ansi.rs`.
Aggiunte asserzioni T2 e T3 in `engine_test.rs` che verificano il tipo.

## Perché
Stabilizza il modello di cella della griglia terminale; il rendering degli
attributi arriverà nel Plan 2. Segue il ciclo TDD: asserzione fallisce →
implementazione → passa (CONSOLE_TEST_PASS verificato).

## File toccati
- kernel/src/console/ansi.rs
- kernel/src/console/engine_test.rs
