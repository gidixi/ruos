# 229 — Grid core: cells, cursor, dirty tracking

**Data:** 2026-06-03

## Cosa
Aggiunto `kernel/src/console/grid.rs` con la struttura `Grid`: matrice di celle
`Cell` row-major, cursore `(cur_col, cur_row)`, tracking dirty per riga tramite
span `(min_col, max_col)`. Metodi implementati: `new`, `cell`, `cursor`,
`dirty_span`, `set_fg`/`set_bg`/`set_attr`, `current_colors`, `mark`,
`clear_dirty`, `put`, `cr`, `bs`, `tab`, `newline`, `scroll_up` (stub temporaneo
— Task 5 sostituirà con lo scroll reale). Il modulo è dichiarato in
`kernel/src/console/mod.rs`. Aggiunte asserzioni T7–T12 in `engine_test.rs`
(put/cursor/dirty/cr/newline/bs); `make run-console-test` → `CONSOLE_TEST_PASS`.

## Perché
Task 4 del piano terminal-engine: fondamenta della griglia terminale
pixel-agnostica, prerequisito del renderer (Task 5+) e del parser ANSI (Task 6+).

## File toccati
- kernel/src/console/grid.rs (nuovo)
- kernel/src/console/mod.rs
- kernel/src/console/engine_test.rs
