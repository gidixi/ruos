# 95 — PTY VFS integration (T2)

**Data:** 2026-05-29

## Cosa

- `FileImpl::PtySlave(PtySlaveFile)` variant in vfs/file.rs.
- `PtySlaveFile` in vfs/devices.rs: async read drains pair[idx].
  slave_rx (registers slave_waker on empty); write loops
  ldisc::process_output(b) per byte.
- `TmpKind::PtySlave(usize)` in vfs/tmpfs.rs; tmpfs::open dispatches.
- vfs::init mkdirs /dev/pts and inserts /dev/pts/0..3 inodes.
- boot::phases::fs::init calls pty::init() so PAIRS array is logged.

## Perché

Step 12 T2: PTY slave esposto come VFS file. T3 connetterà keyboard
ISR → master input + console drain task → master output e wirerà
RuntimeState FD 0/1/2 a /dev/pts/0.

## File toccati

- kernel/src/vfs/file.rs
- kernel/src/vfs/devices.rs
- kernel/src/vfs/tmpfs.rs
- kernel/src/vfs/mod.rs
- kernel/src/boot/phases/fs.rs
- CHANGELOG/95-26-05-29-pty-vfs.md (nuovo)
