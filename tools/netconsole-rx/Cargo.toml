[package]
name = "netconsole-rx"
version = "0.1.0"
edition = "2021"
description = "Host-side receiver for ruos netconsole UDP broadcast logs (drop-in for `nc -ul 6666`)."

[[bin]]
name = "netconsole-rx"
path = "src/main.rs"

[dependencies]
