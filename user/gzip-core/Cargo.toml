[package]
name = "gzip-core"
version = "0.1.0"
edition = "2021"

[dependencies]
miniz_oxide = { version = "0.8", default-features = false, features = ["with-alloc"] }

[features]
default = ["std"]
std = []

[lib]
name = "gzip_core"
path = "src/lib.rs"
